# 发布到 PyPI

注册 PyPI 账号，拿到 token
然后对打包的产物 sdist 和 wheel 用 twine 发布上去。

测试就是别人 uv tool install 并且得到一个 cli 可以执行命令，也可以通过 MCP 调用。

[reaper-orchestra · PyPI](https://pypi.org/project/reaper-orchestra/0.1.0/)
